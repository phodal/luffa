sudo: false
language: node_js
node_js:
  - v4
  - '0.12'
notifications:
  email: false
before_install: npm install -g bower;npm install -g grunt-cli;npm install -g browserify;
install: bower install; npm install
